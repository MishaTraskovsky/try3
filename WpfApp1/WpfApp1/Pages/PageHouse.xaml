<Page x:Class="WpfApp1.Pages.PageHouse"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White"
      Title="PageHouse">

    <Grid>
        <DataGrid Name="DGreedHouse" AutoGenerateColumns="False" IsReadOnly="True" ColumnWidth="*" Margin="0,0,0,195">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ЖК" Binding="{Binding ResidentialComplex.Name}"/>
                <DataGridTextColumn Header="Улица" Binding="{Binding Street}"/>
                <DataGridTextColumn Header="Номер дома" Binding="{Binding Number}"/>
                <DataGridTextColumn Header="Статус ЖК" Binding="{Binding ResidentialComplex.Status}"/>
                <DataGridTextColumn Width="130" Header="Проданных квартир" Binding="{Binding Apartment.IsSold.Count}"/>
                <DataGridTextColumn Width="140" Header="Продающихся квартир" Binding="{Binding ResidentialComplex.Name}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
